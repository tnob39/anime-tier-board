# UX_DIRECTION.md — UIUX改善の共有方針（全エージェント必読）

> **対象**: Claude Code / Codex / Grok / Hermes など本プロジェクトに関わる全エージェント
> **位置づけ**: UIUX に関わる実装・レビュー・設計の**唯一の参照元（source of truth）**。
> 個別 Issue より上位の方針。Issue と矛盾したら本ドキュメントを優先し、Issue 側を直す。
> **最終更新**: 2026-06-18 / 決定者: Nobu（オーナー）

---

## 0. このドキュメントの使い方

- UIUX に触れる作業の前に必ず読む。実装方針・ラベル・配置の判断はここに従う。
- 方針が変わったら**このファイルを更新してからコミット**する（口頭・チャットのみの決定を残さない）。
- 具体タスクは GitHub Issues（EPIC #61 / #65 と子Issue）で管理。本書は「なぜ・どの方向に」を定義する。

---

## 1. 問題意識（なぜ刷新するか）

現状の課題（2026-06-13 オーナー評価）:

1. **アプリのアイデンティティが割れている** — ブランドは numanie /「アニメ視聴を自分のものに」（視聴管理）なのに、メインURL `/` が「今期アニメTier表」（作成ツール）を直接表示している。
2. **導線が3か所に分散** — GlobalNav（上部）/ MobileNav（下部）/ HamburgerMenu（ドロワー）。「探す」「声優」が埋没し、どこに何があるか覚えづらい。
3. **各ページのファーストビューが野暮ったい** — グリッド配置中心で、特にスマホで見栄えが悪い。
4. **ユーザーに不要な情報が出ている** — `12作品 / AniList / キャッシュ` のような開発者向けメタ情報、即ログイン壁など。

## 2. デザイン原則（イケてるアプリ基準）

参照する「イケてる」基準: **Letterboxd / AniList / Crunchyroll / Spotify**。

- **ホームは「自分のもの」が映る場所**。アニメ管理アプリのホームは "Continue Watching"（視聴中の続き）が王道。
- **ファーストビューは横スクロールのカードレーン基調**。グリッドの敷き詰めをやめ、レーンで「見せる」。
- **主要導線は下部バー1本に4〜5個まで**。ハンバーガーは設定・アカウント専用。行き先を入れない。
- **1画面1主アクション**。道具箱（ツールバー）を全部見せない。二次操作は ⋯ に畳む。
- **ユーザーにメタ情報を見せない**（データソース名・キャッシュ状態など）。
- **登録は後**。価値を見せる前にログイン壁を出さない（ゲスト閲覧を許容）。
- **シンプル優先**。「シンプルでいい。イケてる画面構成と導線」がオーナーの要望。

---

## 3. 決定済みの方針

### 方針① 視聴中ホーム化（IA再設計） — EPIC #61

`/` を **視聴中ホーム** に変える。Tier は機能の1つとして `/tier` へ移設。

**新IA / ルーティング**

```
/               視聴中ホーム（NEW: おかえり / ▶続きを見る / 🌙今夜の候補 / 🔥今期の注目）
/tier           Tier表（現 / を移設）
/explore        探す（過去作探索 + 生涯ランキング）
/watchlist      視聴中（一覧・詳細管理）
/subscriptions  サブスク
/voice-actors   声優
/dashboard      分析
/settings       設定
```

**下部ナビ**: ~~`ホーム / 探す / Tier / マイ` の4本に集約~~ → **2026-06-16 オーナー判断で変更**。固定4本ではなく、表示モード別に4本（シンプル: ホーム/視聴中/さがす/サブスク、プロ: ホーム/Tier/分析/探索）。声優・更新情報は両モード共通でハンバーガードロワーへ（#107実装済み）。

> **2026-06-18 注記**: 上記は#64実装時に当初案（固定4タブ + マイページ）から変更された。固定4タブ・マイページは不採用。本セクションは実装済みの最新仕様に追従済み。

**ルーティング変更の波及（必ず追従）**:
- `components/MobileNav.tsx` の active 判定（`href === "/"` の特例）
- `components/GlobalNav.tsx` のロゴ遷移先
- **PWA App Shortcuts（#58, manifest）** が `/` を指す箇所
- 共有URL（`/share/...`）・localStorage の storageKey が `/` パス前提でないか

### 方針② ファーストビュー & ページ別UX刷新 — EPIC #65

FV を**横スクロールのカードレーン基調**に刷新する。

> **⚠️ 検証方針（2026-06-13 オーナー指示）**
> レーン基調の刷新は **やってみないと良し悪しが判断できない**。
> いきなり本番ページを書き換えず、**本番に影響しない仮ページ（プロトタイプ）で先に検証する**。
> - 仮ページ例: `/lab/lane` など本番ナビから外したサンドボックスルート（noindex 済み）。
> - 仮ページで `<CardLane>` 共通コンポーネント（#66）とレイアウト案を試し、OK が出てから本番ページへ適用。
> - 検証用の仮ページ・実験コードは本番導線・サイトマップに載せない。採用後に撤去 or `/lab` 配下に隔離。

**ページ別の方向**

| 対象 | 現状の問題 | 方向 | Issue |
|------|-----------|------|-------|
| 共通レーン部品 | グリッド敷き詰め | 再利用可能な `<CardLane>`（横スクロール・スナップ・端フェード） | #66 |
| 放映カレンダー | `WeeklyBroadcastCalendar` の曜日縦グリッドが野暮ったい | 曜日ごとの横スクロールレーンに | #67 |
| タイトル | 長すぎて読みづらい | **AniList `synonyms` で略称化を検証→採用**。必ず「省略+全文（タップ/ツールチップ）」をフォールバックに。※製作委員会名からは取れない | #68 |
| Tier表（スマホ） | S/A/B ラベル横並びが違和感 | モバイルはラベルを行頭チップ＋下に横スクロールの「縦積みレーン」に（PCは現状可） | #69 |
| サブスク | FVが何のページか不明 | 結論ヒーロー化。~~「今 月◯◯円 / ◯◯解約で年◯◯円節約」の円ベース~~ → **実装は「ウォッチリストの◯◯%をカバー中」のカバー率%軸を採用**（2026-06-16実装・2026-06-18文書追認）。一文字サイズ・余白も見直し | #70 |
| 分析 | 文字量が多すぎる | テキストを削り数値・グラフ中心。1スクリーン密度に | #71 |
| 探すアイコン | `Compass`（コンパス） | `Search`（虫眼鏡）に変更 | #72 |
| explore フィルタ | 年+季節の2軸 | **季節を廃止し年単位のみ**（API は seasonYear のみ or 4季merge） | #73 |
| explore トップ | — | **生涯ランキング**（全期間の名作ランキング）+ 作品選択UI を追加。Tierの仕組み流用可 | #74 |

---

## 4. Issue 対応表

| EPIC | 子Issue |
|------|---------|
| **#61** 視聴中ホーム化（IA再設計） | #62 Stage1 ルーティング土台 / #63 Stage2 ホーム本体 / #64 Stage3 ナビ集約・ノイズ除去 |
| **#65** FV & ページ別UX刷新 | #66 レーン共通部品 / #67 カレンダー / #68 略称 / #69 Tierモバイル / #70 サブスクFV / #71 分析 / #72 虫眼鏡 / #73 explore年単位 / #74 生涯ランキング |

**推奨着手順**: #72（極小）→ #66（レーン土台、②は仮ページで検証）→ #61 Stage1〜2（#66を流用）→ 残UX。

---

## 5. エージェント共通の作業規約（UIUX 変更時）

- 実装前に本書 + 対象 Issue を読む。方針逸脱しそうなら先に本書を更新して合意を取る。
- **②レーン刷新は仮ページ検証が必須**。本番ページの直接書き換えから始めない。
- 変更は機能ごとに worktree / ブランチを分け、`npx tsc --noEmit && npm run build` を通してから完了とする。
- ユーザー向けに意味のある変更は `app/updates/page.tsx` の `RELEASES` に追記し最新版に `isLatest: true`（CLAUDE.md ルール）。
- 開発者向けメタ情報（データソース名・キャッシュ・件数デバッグ等）を UI に出さない。
- `Hermes` ファイルはコミット禁止。

---

## 6. 変更履歴

- 2026-06-13: 初版。方針①②を確定。②は仮ページ検証方針を明記（オーナー指示）。
- 2026-06-13: PR #77 で main へ反映（全エージェント共有開始）。EPIC #61 / #65 にも反映済みコメント。
- 2026-06-18: Opus 4.8サブエージェントによるUX_DIRECTION.md vs 実装の監査を実施。本書未追従だった2点を反映:
  - #64 下部ナビ: 固定4タブ案（ホーム/探す/Tier/マイ）を撤回し、モード別4タブ（2026-06-16実装）に更新。
  - #70 サブスクFV: 円ベース節約コピー案を撤回し、カバー率%軸（2026-06-16実装）に更新。
  - 監査で判明した未実装/部分実装（#67放映カレンダー横スクロール化が未実装でIssue再オープン、#72探すアイコンがプロナビ/explore内で未完了でIssue再オープン、#68/#73/#74が未着手、#69/#71が部分実装、explore内のメタ情報表示が原則違反）は本書の方針自体は変更せず、GitHub Issue側で追跡（#67, #69, #70, #71, #72にコメント/再オープン済み）。
